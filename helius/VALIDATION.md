# 本次验证记录

日期：2026-09-07。

- 已安装固定版本依赖并生成锁文件；确认当前依赖树不包含 AllenHark、Shredstream 或 Birdeye。
- 新版源码和启动入口通过 JavaScript 语法检查。
- 55 项本地测试全部通过。包含官方 PumpSwap SDK 1.19.0 构造买卖、签名序列化和 1232 字节交易大小检查（普通池和 cashback 池）。
- 使用合成交易验证 legacy/v0/ALT、CPI、虚拟报价储备、多次 swap 排除、失败/延迟信号排除。
- 使用模拟 RPC 验证 pending 交易持久化、过期后恢复、processed 失败不提前重试、确认后持仓更新、卖空后两小时计划、再次买入取消计划、买入与关闭互斥。
- 验证 SPL Token 和 Token-2022 的 ATA 地址、空余额及关闭权限；没有真实关闭链上账户。

测试环境为本机 Windows。沙箱限制子进程，因此使用以下等价的同进程测试方式；Linux 部署可直接 `npm test`：

```text
node --test --experimental-test-isolation=none helius/test/*.test.js
tests 55 / pass 55 / fail 0
```

未完成的线上验证：没有提供 Helius key、实时交易样本或腾讯云 SSH 会话，故未连接真实行情、未测全天额度、未执行服务器端测速、未向主网发单。Linux systemd 部署脚本尚未在目标服务器执行。默认 DRY_RUN=true。

发行 zip 只包含新版和新版部署文件；旧实现保留在原工作区，没有打入发行包。解压后先按 README 填写 `helius/.env` 并安装依赖。

本次延迟审查增加了重复 ATA 指令检查、过期 blockhash 与池子读取并行测试；修正 Sender 保活响应体处理，并加入精确区分准备/发送响应的日志。本机合成性能测量见 LATENCY.md。发行依赖已移除指向父目录的本地依赖，避免全新安装引入不需要的项目。

新增反弹观察验证：过去特征不含未来数据、延迟入场、窗口外反弹不算窗口内成功、先止损后反弹的标签分离、断流/缺数据/容量不足标记为空、费用减少收益、冷却/满仓/准备额度不隐藏候选、队列有界和不传递密钥。真实 worker 测试验证异步写盘和正常关闭时未完成结果的缺失标记。

离线训练使用合成样本验证时间分区及跨窗口剔除、训练/校准/测试流程、模型加载、配置不匹配拒绝推断、重复/截断/未成熟样本排除。空数据命令行测试返回 insufficient_data，仅写报告且不创建模型。合成数据上的验证通过不代表真实市场可预测；没有生成随包分发的模型。

独立 worker 的 6,400 条合成事件测试未丢队列事件，主线程入队与消息传递摊销 p50 约 0.0015 ms/条，p95 约 0.0028 ms/条；128 个观察候选结束时产生 384 个缺失结果，未伪造成功/失败。此短测不评价长期工作线程吞吐、磁盘增长、实际推断效果或交易落块延迟。

COS 日报新增 8 项测试：北京时间 07:00 边界及美国夏令时日期、窗口筛选与跨日关联、晚写日志补带、截断检测、秘密和已签名字节排除、上传大小/摘要元数据校验、失败窗口保留/重试/历史补传、同日不重复请求，以及本地预览不推进进度。使用模拟 COS 接口，官方 SDK 初始化通过；未向真实 bucket 上传。SDK 固定 3.0.0 并更新锁文件。

补充逐笔模拟数量/成本/持有时间、实盘成交金额/费用、启动参数记录；观察线程记录活跃候选池的可解析行情路径。新记录仅在程序更新后产生，历史缺失不能补造。systemd 定时器模板使用显式 Asia/Shanghai 时间，尚未在目标 Linux 服务器验证。
